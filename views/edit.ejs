<%- include("./partials/header") %>

<h1>Edit Cluck 🐔</h1>

<form action="/clucks/<%= cluck.id %>" method="POST">
  <input type="hidden" name="_method" value="PATCH" />
  <div class="form-row">
    <div class="form-group col-md-6">
      <label for="img_url">Image URL</label>
      <input name="img_url" type="text" class="form-control" id="img_url" value="<%= cluck.img_url %>">
    </div>
    <div class="form-group col-md-6">
      <label for="content">Cluck Content</label>
      <input name="content" type="text" class="form-control" id="content" value="<%= cluck.content %>">
    </div>
  </div>
  <button type="submit" class="btn btn-warning">Edit Cluck</button>
</form>

<%- include("./partials/footer") %>